<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="qtp.list">
      <field name="name">qtp list</field>
      <field name="model">qtp.qtp</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="qtp.action_window">
      <field name="name">qtp window</field>
      <field name="res_model">qtp.qtp</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="qtp.action_server">
      <field name="name">qtp server</field>
      <field name="model_id" ref="model_qtp_qtp"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="qtp" id="qtp.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="qtp.menu_1" parent="qtp.menu_root"/>
    <menuitem name="Menu 2" id="qtp.menu_2" parent="qtp.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="qtp.menu_1_list" parent="qtp.menu_1"
              action="qtp.action_window"/>
    <menuitem name="Server to list" id="qtp" parent="qtp.menu_2"
              action="qtp.action_server"/>
    -->
  </data>
</odoo>